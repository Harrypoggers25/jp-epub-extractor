<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Japanese Dictionary</title>

    <style>
        * {
            box-sizing: border-box;
        }

        :root {
            --bg: #181a1b;
            --bg-panel: #1e2021;
            --bg-input: #131415;
            --bg-item: #232527;
            --bg-item-hover: #272a2b;
            --bg-disabled: #1a1c1d;
            --border: #333;
            --border-subtle: #2a2c2d;
            --border-faint: #262829;
            --text: #e8e6e3;
            --text-muted: #999;
            --text-disabled: #5a5c5d;
            --text-disabled-2: #4a4c4d;
            --shadow: 0 8px 30px rgba(0, 0, 0, .5);
            --shadow-soft: 0 8px 30px rgba(0, 0, 0, .4);

            --accent: #7c8aff;
            --accent-bg: #1b2038;
            --accent-bg-hover: #232b4d;
            --accent-glow: rgba(21, 62, 255, .27);

            --success: #5cbf60;
            --success-bg: #14211a;
            --success-bg-hover: #1b2d23;
            --success-glow: rgba(76, 175, 80, .22);

            --warning: #d9b64c;
            --warning-bg: #332b0f;
            --warning-bg-hover: #3d331a;

            --danger: #e05c5c;
            --danger-bg: #2b1414;
            --danger-bg-hover: #331a1a;

            --jlpt: #ffab40;
        }

        html,
        body {
            margin: 0;
            height: 100%;
            font-family: "Noto Sans JP", Inter, Arial, sans-serif;
            background: var(--bg);
            color: var(--text);
        }

        .search-item:focus,
        .search-item:focus-visible,
        .search-item:active,
        .header-btn:focus,
        .header-btn:focus-visible,
        .header-btn:active,
        .confirm-btn:focus,
        .confirm-btn:focus-visible,
        .confirm-btn:active,
        .entry:focus,
        .entry:focus-visible,
        .entry:active,
        .modal-item:focus,
        .modal-item:focus-visible,
        .modal-item:active {
            outline: none !important;
        }

        .layout {
            display: flex;
            height: 100vh;
        }

        /* ===========================
            Sidebar
        ============================ */

        .sidebar {
            width: 320px;
            background: var(--bg-panel);
            border-right: 1px solid var(--border);
            display: flex;
            flex-direction: column;
        }

        .sidebar-header {
            padding: 20px;
            border-bottom: 1px solid var(--border-subtle);
        }

        .sidebar-header h2 {
            margin: 0 0 16px;
            font-size: 24px;
        }

        .search-input,
        .modal-search {
            width: 100%;
            padding: 12px 14px;
            border: 1px solid #444;
            border-radius: 10px;
            font-size: 15px;
            outline: none;
            background: var(--bg-input);
            color: var(--text);
        }

        .modal-search {
            padding: 10px 12px;
            font-size: 14px;
            margin-bottom: 14px;
        }

        .search-input:focus,
        .modal-search:focus {
            border-color: var(--accent);
        }

        .search-results {
            flex: 1;
            overflow-y: auto;
        }

        .search-item {
            --row-accent: var(--text-muted);
            --row-bg: transparent;
            --row-bg-hover: var(--bg-item-hover);

            display: flex;
            justify-content: space-between;
            padding: 14px 20px 14px 16px;
            border-bottom: 1px solid var(--border-faint);
            border-left: 4px solid var(--row-accent);
            background: var(--row-bg);
            cursor: pointer;
            transition: background .2s;
            color: var(--text);
        }

        .search-item:is(:hover, :focus) {
            background: var(--row-bg-hover);
            color: var(--row-accent);
        }

        .search-item.unique {
            --row-accent: var(--warning);
            --row-bg: var(--warning-bg);
            --row-bg-hover: var(--warning-bg-hover);
        }

        .search-item.error {
            --row-accent: var(--danger);
            --row-bg: var(--danger-bg);
            --row-bg-hover: var(--danger-bg-hover);
        }

        .search-item.active {
            --row-accent: var(--accent);
            --row-bg: var(--accent-bg);
            --row-bg-hover: var(--accent-bg-hover);
        }

        .search-item.unsure,
        .search-item.modified {
            --row-accent: var(--success);
            --row-bg: var(--success-bg);
            --row-bg-hover: var(--success-bg-hover);
        }

        .search-item.unique:is(:hover, :focus),
        .search-item.error:is(:hover, :focus),
        .search-item.active:is(:hover, :focus),
        .search-item.unsure:is(:hover, :focus),
        .search-item.modified:is(:hover, :focus) {
            border-left-color: var(--row-accent);
        }

        .search-item.merged .search-tag-merged,
        .search-item.unsure .search-tag-unsure,
        .search-item.ignore .search-tag-ignore {
            color: var(--text);
        }

        .search-word {
            font-size: 20px;
            font-weight: bold;
        }

        .search-word-type {
            color: var(--text-muted);
            margin-top: 3px;
            font-size: 14px;
        }

        .search-item-tags {
            display: flex;
            flex-direction: column;
        }

        .search-item-tags>div {
            align-self: center;
            color: #444;
            font-size: 12px;
            font-weight: bold;
        }

        .sidebar-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 15px;
            padding: 10px;
            font-size: 15px;
            font-weight: bold;
            border-top: 1px solid var(--border-subtle);
        }

        .confirm-btn {
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 13px;
            cursor: pointer;
            border: 1px solid var(--success);
            background: var(--success-bg);
            color: var(--success);
            transition: background .2s, border-color .2s;
        }

        .confirm-btn:is(:hover, :focus) {
            background: var(--success-bg-hover);
        }

        .confirm-btn:disabled,
        .confirm-btn:disabled:is(:hover, :focus) {
            background: var(--bg-disabled);
            color: var(--text-disabled);
            border-color: var(--border-subtle);
            cursor: default;
        }

        /* ===========================
            Content
        ============================ */

        .content {
            flex: 1;
            overflow-y: auto;
            padding: 40px;
        }

        .container {
            max-width: 900px;
            margin: auto;
        }

        .header {
            background: var(--bg-panel);
            padding: 24px;
            border-radius: 14px;
            margin-bottom: 24px;
            box-shadow: var(--shadow-soft);
            border: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            gap: 20px;
            align-items: stretch;
        }

        .header-actions {
            display: flex;
            flex-direction: column;
            gap: 8px;
            justify-content: center;
            flex-shrink: 0;
        }

        .header-btn {
            background: var(--bg-item);
            color: var(--text);
            border: 2px solid var(--border);
            border-radius: 8px;
            padding: 8px 14px;
            font-size: 13px;
            cursor: pointer;
            white-space: nowrap;
            text-align: left;
            transition: background .2s, border-color .2s;
        }

        .header-btn:is(:hover, :focus) {
            background: var(--bg-item-hover);
            border-color: var(--accent);
            box-shadow: var(--shadow-soft), 0 8px 30px var(--accent-glow);
            color: var(--accent);
        }

        .header-btn.selected {
            border-color: var(--success);
        }

        .header-btn.selected:is(:hover, :focus) {
            color: var(--success);
            box-shadow: 0 8px 30px var(--success-glow);
        }

        .header-btn:disabled,
        .header-btn:disabled:is(:hover, :focus) {
            background: var(--bg-disabled);
            color: var(--text-disabled);
            border-color: var(--border-subtle);
            cursor: default;
            box-shadow: none;
        }

        .header-content {
            flex: 1;
            min-width: 0;
        }

        .header h1 {
            margin: 0;
            font-size: 38px;
        }

        .meta {
            margin-top: 10px;
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .badge {
            background: var(--accent-bg);
            color: var(--accent);
            padding: 6px 12px;
            border-radius: 999px;
            font-size: 14px;
            align-content: center;
        }

        .badge.hidden {
            display: none;
        }

        #mergeCount {
            background: var(--warning-bg);
            color: var(--warning);
        }

        #mergeWith {
            background: var(--success-bg);
            color: var(--success);
        }

        .entry {
            background: var(--bg-panel);
            border-radius: 14px;
            padding: 22px;
            margin-bottom: 20px;
            border: 2px solid var(--border);
            box-shadow: var(--shadow);
            cursor: pointer;
        }

        .entry:is(:hover, :focus) {
            background: var(--bg-item-hover);
            border-color: var(--accent);
            box-shadow: var(--shadow-soft), 0 8px 30px var(--accent-glow);
        }

        .entry:is(:hover, :focus) .slug {
            color: var(--accent);
        }

        .entry.selected {
            border-color: var(--success);
        }

        .entry.selected:is(:hover, :focus) {
            box-shadow: 0 8px 30px var(--success-glow);
        }

        .entry.selected:is(:hover, :focus) .slug {
            color: var(--success);
        }

        .entry-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .entry.disabled {
            background: var(--bg-disabled);
            border-color: var(--border-subtle);
            cursor: default;
            box-shadow: none;
        }

        .entry.disabled:is(:hover, :focus) {
            background: var(--bg-disabled);
            border-color: var(--border-subtle);
            box-shadow: none;
        }

        .entry.disabled .slug,
        .entry.disabled:is(:hover, :focus) .slug {
            color: var(--text-disabled);
        }

        .entry.disabled.selected,
        .entry.disabled.selected:is(:hover, :focus) {
            border-color: #2f4a31;
            box-shadow: none;
        }

        .entry.disabled.selected:is(:hover, :focus) .slug {
            color: var(--text-disabled);
        }

        .entry.disabled .section h3 {
            color: var(--text-disabled-2);
        }

        .entry.disabled .word {
            background: #1f2122;
        }

        .entry.disabled .word strong {
            color: var(--text-disabled);
        }

        .entry.disabled .word span {
            color: var(--text-disabled-2);
        }

        .entry.disabled .sense {
            background: var(--bg-input);
            border-left-color: #3a3d47;
        }

        .entry.disabled .definitions {
            color: var(--text-disabled);
        }

        .entry.disabled .tags .tag {
            background: #202122;
            color: #6a6c6d;
        }

        .entry.disabled .common {
            background: #2f4a31;
            color: var(--text-disabled);
        }

        .entry.disabled .jlpt {
            background: #4a3b1f;
            color: var(--text-disabled);
        }

        .slug {
            font-size: 30px;
            font-weight: bold;
        }

        .common {
            background: var(--success);
            color: #0d0f0f;
            padding: 5px 12px;
            border-radius: 999px;
            font-size: 13px;
            font-weight: 500;
        }

        .jlpt {
            background: var(--jlpt);
            color: #0d0f0f;
            padding: 5px 12px;
            border-radius: 999px;
            margin-left: 8px;
            font-size: 13px;
            font-weight: 500;
        }

        .section {
            margin-top: 18px;
        }

        .section h3 {
            margin-bottom: 10px;
            color: var(--text-muted);
            font-size: 15px;
            text-transform: uppercase;
            letter-spacing: .08em;
        }

        .word {
            display: inline-block;
            margin: 6px;
            padding: 10px 14px;
            background: var(--bg-item);
            border-radius: 10px;
        }

        .word strong {
            display: block;
            font-size: 22px;
        }

        .word span {
            color: var(--text-muted);
        }

        .sense {
            padding: 12px;
            border-left: 4px solid #7986cb;
            background: #16181d;
            margin-bottom: 10px;
        }

        .definitions {
            font-size: 17px;
            margin-bottom: 8px;
        }

        .tags {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-top: 8px;
        }

        .tag {
            background: #2c2c2c;
            border-radius: 999px;
            padding: 4px 10px;
            font-size: 13px;
            color: #ccc;
        }

        /* ===========================
            Overlay Base
        ============================ */

        .overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, .6);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 1000;
        }

        .overlay.open {
            display: flex;
        }

        /* ===========================
            Modal
        ============================ */

        .modal-box {
            background: var(--bg-panel);
            border: 1px solid var(--border);
            border-radius: 14px;
            padding: 20px;
            width: 100%;
            max-width: 420px;
            max-height: 70vh;
            box-shadow: var(--shadow);
            display: flex;
            flex-direction: column;
        }

        .modal-box h3 {
            margin: 0 0 14px;
            font-size: 18px;
            flex-shrink: 0;
        }

        .modal-box h3 span {
            color: var(--accent);
        }

        .modal-list {
            overflow-y: auto;
            border: 1px solid var(--border-faint);
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .modal-item {
            padding: 12px 16px;
            border-bottom: 1px solid var(--border-faint);
            border-left: 4px solid var(--text-muted);
            cursor: pointer;
            transition: background .2s;
            color: var(--text);
        }

        .modal-item:last-child {
            border-bottom: none;
        }

        .modal-item:is(:hover, :focus) {
            background: var(--bg-item-hover);
            border-left-color: var(--text-muted);
            color: var(--text-muted);
        }

        .modal-item.selected {
            background: var(--accent-bg);
            border-left-color: var(--accent);
        }

        .modal-item.selected:is(:hover, :focus) {
            background: var(--accent-bg-hover);
            border-left-color: var(--accent);
            color: var(--accent);
        }

        .modal-item-word {
            font-size: 16px;
            font-weight: bold;
        }

        .modal-item-type {
            color: var(--text-muted);
            margin-top: 2px;
            font-size: 13px;
        }

        .modal-actions {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            flex-shrink: 0;
        }

        .modal-btn {
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 13px;
            cursor: pointer;
            border: 1px solid var(--border);
            background: var(--bg-item);
            color: var(--text);
            transition: background .2s, border-color .2s;
        }

        .modal-btn:hover {
            background: var(--bg-item-hover);
            border-color: var(--accent);
        }

        .modal-btn.confirm {
            background: var(--accent-bg);
            border-color: var(--accent);
            color: var(--accent);
        }

        .modal-btn.confirm:hover {
            background: var(--accent-bg-hover);
        }

        .modal-btn.confirm:disabled {
            background: var(--bg-disabled);
            color: var(--text-disabled);
            border-color: var(--border-subtle);
            cursor: default;
        }

        #sentenceModal .modal-box {
            max-width: 80vw;
            max-height: 75vh;
        }

        #sentenceModal .modal-list {
            display: flex;
            flex-direction: row-reverse;
            overflow-x: auto;
            overflow-y: hidden;
            height: 50vh;
            padding: 10px 6px;
        }

        #sentenceModal .modal-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            flex-shrink: 0;
            border-bottom: none;
            border-left: none;
            border-right: 4px solid var(--text-muted);
            padding: 8px 14px;
            margin-right: 20px;
            height: 100%;
        }

        #sentenceModal .modal-item:is(:hover, :focus) {
            border-right: 4px solid var(--text-muted);
        }

        #sentenceModal .modal-item.selected,
        #sentenceModal .modal-item.selected:is(:hover, :focus) {
            border-right: 4px solid var(--accent);
        }

        #sentenceModal .modal-item-header {
            writing-mode: horizontal-tb;
            color: var(--text-muted);
            font-size: 12px;
            margin-bottom: 8px;
            flex-shrink: 0;
        }

        #sentenceModal .modal-item-text {
            writing-mode: vertical-rl;
            text-orientation: mixed;
            font-size: 16px;
            line-height: 1.9;
            letter-spacing: 0.05em;
            height: 100%;
            color: var(--text);
        }

        #sentenceModal .modal-item-text b {
            color: var(--accent);
        }

        /* ===========================
            Modal
        ============================ */

        #confirmOverlay.overlay {
            background: rgba(0, 0, 0, .9);
        }

        .overlay-box {
            display: flex;
            align-items: center;
            flex-direction: column;
        }

        .overlay-box h3 {
            margin-top: 0;
            font-size: 40px;
        }

        .overlay-box h3 span {
            font-weight: 500;
        }

        .overlay-box span {
            font-weight: 500;
        }
    </style>
</head>

<body>
    <div class="layout">
        <aside class="sidebar">
            <div class="sidebar-header">
                <h2>Word list</h2>
                <input id="searchInput" class="search-input" type="text" placeholder="Search Japanese..."
                    autocomplete="off">
            </div>
            <div id="searchResults" class="search-results">
                <!-- <div class="search-item"> -->
                <!--     <div class="search-word">買い物帰り</div> -->
                <!--     <div class="search-word-type">かいものがえり</div> -->
                <!-- </div> -->
            </div>
            <div class="sidebar-footer">
                <span id="searchResultCount"></span>
                <button class="confirm-btn" id="btnConfirm" disabled>Confirm</button>
            </div>
        </aside>
        <main class="content">
            <div class="container">
                <div class="header">
                    <div class="header-content">
                        <h1 id="basicForm"></h1>
                        <div class="meta">
                            <div id="tokenId" class="badge"></div>
                            <div id="wordType" class="badge"></div>
                            <div id="count" class="badge"></div>
                            <div id="occurrence" class="badge"></div>
                        </div>
                        <div class="meta">
                            <div id="mergeCount" class="badge hidden"></div>
                            <div id="mergeWith" class="badge hidden"></div>
                        </div>
                    </div>
                    <div id="headerActions" class="header-actions">
                        <!-- <button class="header-btn" id="btnUnimportant">Unimportant</button> -->
                        <!-- <button class="header-btn" id="btnIgnore">Ignore</button> -->
                        <!-- <button class="header-btn" id="btnMerge">Merge with...</button> -->
                    </div>
                </div>
                <div id="entries"></div>
            </div>
        </main>
    </div>
    <div class="overlay" id="mergeModal">
        <div class="modal-box">
            <h3>Selected word: <span id="mergeModalTargetWord"></span></h3>
            <h3>Merge with: <span id="mergeModalSelectedWord"></span></h3>
            <input id="mergeModalSearchInput" class="modal-search" type="text" placeholder="Search entries..."
                autocomplete="off">
            <div class="modal-list" id="mergeModalList">
                <!-- <div class="modal-item"> -->
                <!--     <div class="modal-item-word">動く</div> -->
                <!--     <div class="modal-item-type">動詞</div> -->
                <!-- </div> -->
            </div>
            <div class="modal-actions">
                <button class="modal-btn" id="mergeModalCancel">Cancel</button>
                <button class="modal-btn confirm" id="mergeModalConfirm">Confirm</button>
            </div>
        </div>
    </div>
    <div class="overlay" id="sentenceModal">
        <div class="modal-box">
            <h3>Reference word: <span id="sentenceModalReferenceWord"></span></h3>
            <div class="modal-list" id="sentenceModalList">
                <!-- <div class="modal-item"> -->
                <!--     <div class="modal-item-header">1:1</div> -->
                <!--     <div class="modal-item-text">私はリンゴが好きです</div> -->
                <!-- </div> -->
            </div>
        </div>
    </div>
    <div class="overlay" id="confirmOverlay">
        <div class="overlay-box">
            <h3>Loading: <span id="confirmOverlayProgress"></span></h3>
            <span class="overlay-message" id="confirmOverlayMessage"></span>
        </div>
    </div>
    <script type="module" src="/tools.helper.js"></script>
    <script type="module" src="/api.helper.js"></script>
    <script type="module" src="/review.js"></script>
</body>

</html>
